// Ejercicio 5: Longitud de array
// Crea una función genérica que reciba un array de cualquier tipo y devuelva su longitud como número.


const fnTipos = <T>(value: T[]): number => {
    return value.length
}